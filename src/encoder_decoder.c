#include "stdio.h"
#include "stdlib.h"
#include "encoder_decoder.h"

webPlayerInfo *decode(char *info) {
    webPlayerInfo *result = malloc(sizeof(webPlayerInfo));
    // pisz tutaj!

    return result;
}

char *encode(webPlayerInfo info) {
    char *result = malloc(sizeof(char) * 64);
    // pisz tutaj!

    return result;
}